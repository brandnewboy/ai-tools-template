export enum StoreIdKey {
  appBase = 'app-base-store-id',
  ACCESS_TOKEN = 'baidu_ai_access_token',
  AI_DRAW_IMAGE_RES = 'ai_draw_image_results'
}

export enum TabBar {
  ImageIdentify = 0,
  DrawImage = 1
}
